apiVersion: v1
kind: Service
metadata:
  name: analytics-collector
  labels:
    app.kubernetes.io/name: analytics-collector
    app.kubernetes.io/part-of: huly
spec:
  selector:
    app.kubernetes.io/name: analytics-collector
  ports:
    - name: http
      port: 4017
      targetPort: http
