apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: huly-oauth2-proxy
  labels:
    app.kubernetes.io/part-of: huly
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - huly.hz.ledoweb.com
      secretName: huly-tls
  rules:
    - host: huly.hz.ledoweb.com
      http:
        paths:
          - path: /_oauth2
            pathType: Prefix
            backend:
              service:
                name: oauth2-proxy
                port:
                  number: 4180
